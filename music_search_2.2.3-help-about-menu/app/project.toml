# pyproject.toml (Beispiel)
[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "my-qt-app"
version = "0.1.0"
description = "Qt‑App mit sauberer Architektur"
authors = [{name = "Dein Name", email = "you@example.com"}]
dependencies = [
    "PySide6>=6.7",
    # weitere Abhängigkeiten …
]

[project.scripts]
myqtapp = "src.app:run"